# 隐私政策（Privacy Policy）

**生效日期：2026-09-09**

本隐私政策适用于浏览器扩展 **Media Catch（媒体抓取器）**（以下简称"本扩展"）。本扩展是一款开源浏览器扩展，遵循最小化权限原则运行。

## 一、数据收集

本扩展**不收集、不传输、不上传任何个人数据**。具体说明如下：

| 数据类型 | 是否收集 | 说明 |
| --- | --- | --- |
| 个人身份信息（姓名、邮箱、账号等） | ❌ 不收集 | 本扩展不要求登录，不读取任何个人信息 |
| 浏览历史 | ❌ 不收集 | 仅读取**当前活动标签页**的网络请求信息 |
| 媒体资源信息（URL、类型、大小） | ✅ 仅在本地临时处理 | 用于在当前页面弹出列表中展示可下载的媒体资源 |
| 鉴权头（Referer / Cookie / Authorization / `x-*`） | ✅ 仅在本地捕获、仅本地使用 | 随媒体请求一并被捕获，**仅用于本机下载与预览时的请求头注入**，绝不上传、绝不外发 |
| 地理位置 | ❌ 不收集 | — |
| 设备信息 | ❌ 不收集 | — |

## 二、数据存储位置与保留时间

- **临时资源数据**（抓取到的资源列表）存储在本地会话存储 `chrome.storage.session`，标签页关闭、刷新或浏览器重启后自动清除；
- **用户偏好**存储在本地存储 `chrome.storage.local`（如 `captureEnabled` / `captureMode` / `downloadSaveAs` / `fileNameTemplate` / `aria2Rpc`），仅存于本机、不随账号同步；
- **规则配置**存储在同步存储 `chrome.storage.sync`（`userRules`：扩展名 / MIME / 正则 / URL 黑白名单四张表），可能随浏览器账号同步；
- 以上数据仅用于在当前标签页展示资源列表，以及执行用户主动触发的操作；
- 本扩展**没有任何服务器、没有后端服务、不主动上传任何数据**（除用户主动触发的下载 / 解析等本机请求外）。

## 三、权限说明（最小化原则）

| 权限 | 用途 |
| --- | --- |
| `webRequest` | 非阻塞监听网络请求，识别当前页面的媒体资源（MV3 下合规的唯一采集路径） |
| `downloads` | 触发浏览器原生下载功能 |
| `storage` | 临时资源列表（`storage.session`）、用户偏好（`storage.local`）、规则配置（`storage.sync`） |
| `alarms` | 定时唤醒 Service Worker，维持监听注册 |
| `webNavigation` | 页面跳转时清理对应标签页的临时数据 |
| `declarativeNetRequest` | 会话规则注入 Referer / Cookie 等请求头（用于本机预览与下载时的防盗链） |
| `contextMenus` | 右键菜单（清空本页 / 切换深度模式 / 暂停抓取 / 下载此图片） |
| `scripting` | 仅深度搜索模式下注入 MAIN world 脚本，用于发现动态拼接的媒体 URL |
| `sidePanel` | 侧边栏面板（与弹窗共用 `popup.html`） |
| `tabCapture` | 标签页录制：把已渲染的标签页画面与声音录成 `.webm`（需用户主动点击开始） |
| `host_permissions: <all_urls>` | 允许监听来自第三方 CDN 的媒体资源请求 |

以上权限均为实现核心功能所必需，未申请任何与功能无关的权限。

### 关于标签页录制（v0.3.3 新增）

本扩展提供「录制当前标签页」能力（依赖 `tabCapture` 权限）：将**已渲染的标签页画面与声音**在本地录制成 `.webm` 文件。

- 录制**全程在本机完成，媒体数据不离开本机、绝不上传**；
- 录制必须由**用户主动点击「开始录制」**才会启动，不会后台静默录制；
- 受版权保护的加密（DRM）内容会录成黑屏，属浏览器安全限制。

## 四、第三方服务

本扩展**不包含**任何第三方 SDK、广告、统计、分析、追踪代码，也不与任何第三方共享数据。

## 五、版权声明

本扩展**仅嗅探并保存网页中公开可访问、未加密的媒体资源**：
- 不解析加密流（DRM / Widevine / EME）；
- 不绕过任何技术保护措施（防盗链、加密、DRM）；
- 不提供任何"破解""去版权"能力。

下载内容仅供个人学习与研究使用。用户须自行确保对下载内容的使用符合《中华人民共和国著作权法》及所在司法管辖区的相关法律规定，并自行承担因使用下载内容产生的全部法律责任。

## 六、儿童隐私

本扩展不针对儿童设计，也不收集任何年龄相关数据。

## 七、政策变更

本隐私政策如有更新，将在本页面公布最新版本并更新生效日期。重大变更将通过扩展商店页面公告。

## 八、联系我们

如对本隐私政策有任何疑问，请通过本项目 GitHub 仓库提交 Issue：
https://github.com/HELIX-AISTUDIO/media-capture-extension/issues
